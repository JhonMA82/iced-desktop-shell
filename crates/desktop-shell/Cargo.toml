[package]
name = "desktop-shell"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Reusable technical desktop shell infrastructure (commands, dock, ribbon, theme, persistence)"

[dependencies]
iced = "0.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
directories = "6.0"
tracing = "0.1"
