[package]
name = "scaffold"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Declarative scaffolding model, validation, and renderer for iced-desktop-shell application presets (zero external dependencies)"
